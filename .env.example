# ── Firebase (client — exposées au navigateur, valeurs "publiques" par design) ──
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_DATABASE_URL=

# ── Firebase Admin (serveur uniquement — SECRET, ne jamais exposer au client) ──
# Trouvable dans Firebase Console > Paramètres du projet > Comptes de service
# > Générer une nouvelle clé privée.
FIREBASE_ADMIN_PROJECT_ID=
FIREBASE_ADMIN_CLIENT_EMAIL=
# Coller la clé privée telle quelle. Si Windows/cmd transforme les retours à la
# ligne, remplacer chaque vrai retour à la ligne par le texte littéral \n
# (le code gère déjà ce cas, voir lib/firebase/admin.ts).
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ── Email transactionnel (SendGrid — mot de passe oublié, notifications) ──
# Sans ces deux variables, les emails ne partent pas (l'app continue de
# fonctionner mais logue une erreur de configuration plutôt que d'envoyer).
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=

# ── Supabase (branche de migration — voir supabase/migrations/) ──
# Client (exposées au navigateur, valeurs "publiques" par design) :
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Serveur uniquement — SECRET, ne jamais exposer au client. Trouvable dans
# Supabase Dashboard > Project Settings > API > service_role key.
SUPABASE_SERVICE_ROLE_KEY=
